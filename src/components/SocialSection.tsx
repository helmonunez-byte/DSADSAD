@@ .. @@
             href={social.url === '#' ? `https://${social.name.toLowerCase()}.com/40sabores` : social.url}
+            href={social.url === '#' ? `https://${social.name.toLowerCase()}.com/centraldaweb` : social.url}
             target="_blank"
             rel="noopener noreferrer"